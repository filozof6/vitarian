{% extends 'VitarianReceptyBundle::layout.html.twig' %}
{% block body -%}
    <h1>Newsletter list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Email</th>
                <th>Active</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('newsletter_show', { 'id': entity.id }) }}">{{ entity.email }}</a></td>
                <td>{{ entity.active }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('newsletter_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('newsletter_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('newsletter_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
