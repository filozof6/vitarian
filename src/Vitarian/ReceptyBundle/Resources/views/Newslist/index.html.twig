{% extends 'VitarianReceptyBundle::layout.html.twig' %}

{% block body -%}
    <h1>Newslist list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datetime</th>
                <th>Title</th>
                <th>Text</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('newslist_show', { 'id': entity.id }) }}">{{ entity.datetime }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.text }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('newslist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('newslist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('newslist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
