<div class="collapse navbar-collapse">
<ul  class="nav navbar-nav">
    <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="{{ path('post') }}/Recipes">Recipes <span class="caret"></span></a>
        {%  if recipes is defined %}
        <ul  class="dropdown-menu">
        {% for item in recipes %}
            <li><a href="{{ path('post') }}/{{ item.name }}">{{ item.name }}</a></li>
        {% endfor %}
        </ul>
        {% endif %}
    </li>
    <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="{{ path('post') }}/Articles">Articles <span class="caret"></span></a>
        {%  if articles is defined %}
        <ul class="dropdown-menu">
        {% for item in articles %}
                <li><a href="{{ path('post') }}/{{ item.name }}">{{ item.name }}</a></li>
        {% endfor %}
        </ul>
        {% endif %}
    </li>
    <li><a href="{{ path('VitarianReceptyBundle_aboutus') }}">About us</a></li>
    <li><a href="{{ path('VitarianReceptyBundle_contact') }}">Contact</a></li>
    <li><a href="{{ path('catalog_index') }}">Links</a></li>
    <li><a href="{{ path('geo_index') }}">Map</a></li>

  {# {% for menu in menu_links if menu.active %}
    <li><a href="{{ menu.href }}">{{ menu.name }}</a>
    {% endfor %} #}
</ul>
    <div class="nav navbar-nav navbar-right" style="color:white">
         {{ render(controller('VitarianReceptyBundle:Login:index')) }}
    </div>
 </div><!--/.nav-collapse -->